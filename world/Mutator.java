/**
 * A mutator takes two DNA instances and generates a third, which is the one
 * used by the next generation of creatures. Given randomness and other factors
 * the created instance may be exactly the same as the parent DNA.
 */
